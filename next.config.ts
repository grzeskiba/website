import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  output: 'export',
  basePath: '/grzeskiba/website',
  assetPrefix: '/grzeskiba/website/',
  /* config options here */
};

export default nextConfig;
